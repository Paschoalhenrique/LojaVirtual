spring:
  datasource:
    url: jdbc:h2:mem:lojavirtualdb
    driverClassName: org.h2.Driver
    username: sa
    password: 186
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080